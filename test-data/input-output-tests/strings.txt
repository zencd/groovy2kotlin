class Strings {
    static void main() {
        def jStringEscape = '\\d\\d'
        def gStringEscape = "\\d\\d"
        def regex = /[-]?\d\d+/
        def gString = "$a$b${toString()}text$c\\d"
    }
}
-----------
$DEFAULT_IMPORTS
open class Strings {
    companion object {
        fun main() {
            val jStringEscape = "\\d\\d"
            val gStringEscape = "\\d\\d"
            val regex = "[-]?\\d\\d+"
            val gString = "${a}${b}${toString()}text${c}\\d"
        }
    }
}